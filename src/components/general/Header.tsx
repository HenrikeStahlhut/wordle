import { Box, Typography } from "@mui/material";

const Header = () => {
  return (
    <Box margin={"2rem 0"}>
      <Typography variant="h1">Wordle</Typography>
    </Box>
  );
};

export default Header;
